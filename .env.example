# Copy this file to .env and fill in real values.
# .env is gitignored; .env.example is committed as the schema.

# Where to reach the genome backend.
# - Local dev:    http://localhost:8080
# - Live AWS:     https://94rg4ygpy4.ap-south-1.awsapprunner.com
# Code under src/services/api/ appends "/api/v1/..." per endpoint, so this
# value should be just the host root (no trailing slash, no /api/v1 suffix).
VITE_API_BASE_URL=https://94rg4ygpy4.ap-south-1.awsapprunner.com

# App Configuration
VITE_APP_NAME=Bharat Mithra
VITE_APP_VERSION=1.0.0
